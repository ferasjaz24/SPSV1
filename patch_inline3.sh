sed -i 's/    <div\n      onClick={() => setIsEditing(true)}\n      className={`relative cursor-pointer hover:bg-slate-100 hover:ring-1 hover:ring-slate-300 rounded px-1 py-0.5 min-w-\[2rem\] text-center transition-all ${className}`}\n      title="انقر للتعديل"\n    >\n      {type === "number" ? (Number(value) || 0).toLocaleString('\''en-US'\'') : value}\n    <\/div>/    <div\n      onClick={() => setIsEditing(true)}\n      className={`relative cursor-pointer hover:bg-slate-100 hover:ring-1 hover:ring-slate-300 rounded px-1 py-0.5 min-w-\[2rem\] text-center transition-all ${className} ${mod ? "bg-amber-50\/50 ring-1 ring-amber-200" : ""}`}\n      title={mod ? `تعديل! السابق: ${mod.oldValue}\\nبواسطة: ${mod.modifiedBy}\\nانقر للتعديل` : "انقر للتعديل"}\n    >\n      {displayVal}\n      {mod \&\& (\n        <span className="absolute -top-1 -right-1 w-2 h-2 rounded-full bg-amber-400 border border-white"><\/span>\n      )}\n    <\/div>/g' src/components/finance/MonthlyPayrollRuns.tsx
