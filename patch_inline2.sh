sed -i 's/    return <span className={className}>{type === "number" ? (Number(value) || 0).toLocaleString('\''en-US'\'') : value}<\/span>;/  const displayVal = type === "number" ? (Number(value) || 0).toLocaleString('\''en-US'\'') : value;\n\n  if (disabled) {\n    return (\n      <div className="relative inline-block">\n        <span className={className}>{displayVal}<\/span>\n        {mod \&\& (\n          <span title={`السابق: ${mod.oldValue}\\nبواسطة: ${mod.modifiedBy}`} className="absolute -top-1 -right-1 w-2 h-2 rounded-full bg-amber-400 border border-white cursor-help"><\/span>\n        )}\n      <\/div>\n    );\n  }/g' src/components/finance/MonthlyPayrollRuns.tsx
