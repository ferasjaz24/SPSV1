sed -i 's/                      {selectedRun.status === "Pending Final Approval" && isReviewer && (/                      {canRevertToDraft \&\& selectedRun.status !== "Draft" \&\& selectedRun.status !== "Needs Modification" \&\& selectedRun.status !== "Under Modification" \&\& (\n                        <button\n                          onClick={handleRevertToDraft}\n                          className="px-5 py-2 bg-red-100 hover:bg-red-200 text-red-700 font-extrabold rounded-xl text-xs shadow-sm transition-all flex items-center gap-1.5 font-arabic"\n                        >\n                          <AlertCircle className="w-4 h-4" />\n                          إرجاع التعديلات إلى المسودة ↩\n                        </button>\n                      )}\n\n                      {selectedRun.status === "Pending Final Approval" \&\& isReviewer \&\& (/g' src/components/finance/MonthlyPayrollRuns.tsx
